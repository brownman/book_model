<% title "Pages" %>

<table>
  <tr>
    <th>Number</th>
    <th>Chapter</th>
    <th>Lang</th>
  </tr>
  <% for page in @pages %>
    <tr>
      <td><%= page.number %></td>
      <td><%= page.chapter_id %></td>
      <td><%= page.lang_id %></td>
      <td><%= link_to "Show", page %></td>
      <td><%= link_to "Edit", edit_page_path(page) %></td>
      <td><%= link_to "Destroy", page, :confirm => 'Are you sure?', :method => :delete %></td>
    </tr>
  <% end %>
</table>

<p><%= link_to "New Page", new_page_path %></p>
